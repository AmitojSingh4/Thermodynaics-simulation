# size of lattice (n x n)
n <- 10

# number of quanta / pixel (integer)
Q <- 1

# make initial lattice
A <- array(Q, dim = c(n, n))

J <- 1


Energy <- function(i,j) {
  A[i]
}

x <- array(1:20, dim=c(4,5))
print (x)
print(x[4,2])